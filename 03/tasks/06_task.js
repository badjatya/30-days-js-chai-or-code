// Write a program that uses the ternary operator to check if a number a even or odd and log the result, to the console.

const num = 7;

const result = num % 2 === 0 ? "even" : "odd";

console.log(result);
