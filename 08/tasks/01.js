// Use template literals to create a string that includes variables for a person's name and age, a and log the string to the console.

const name = "Archit";
const age = 23;

const str = `My name is ${name} and I am ${age} years old.`;
console.log(str);
